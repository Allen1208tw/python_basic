# 題目 1：讀取檔案並統計資料
#
# 請讀取 Module8/data.txt，並統計檔案內容。
#
# 要求：
# 1. 讀取 data.txt。
# 2. 統計總共有幾行。
# 3. 統計總共有幾個字元。
# 4. 將每一行去掉換行符號後印出來。
#
# 提示：
# - 可以使用 with open("data.txt", "r", encoding="utf-8") as f。
# - 可以用 readlines() 取得所有行。
# - 每一行可以使用 strip() 去掉前後空白與換行。
# - 字元數可以用 len(line) 計算。
# - 如果從 Module8 資料夾外執行，檔案路徑可能要寫成 "Module8/data.txt"。

